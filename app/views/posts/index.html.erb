<h1>All posts</h1>
<%= link_to 'Create Post', controller: 'posts', action: 'new' %>
<p>
<% @posts.each do |post| %>
      <h3><%= post.title %></h3>
      <p><%= post.text %></p>
      <%= link_to 'Show', post_path(post) %>
<% end %>
</p>